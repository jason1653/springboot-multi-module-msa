server:
  port: 8081


spring:
  config:
    import:
      - jasypt.yml
  application:
    name: boot-base
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ENC(cWkTK+UZ0bF37PVwTUOL3w==)
    password: ENC(hBUNHfN+4QfakqGISRfKRKx9+Ipjyr15)
  jpa:
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        highlight_sql: true
        format_sql: true
        show_sql: true
        use_sql_comments: true
aes:
  key: "Test1234"
